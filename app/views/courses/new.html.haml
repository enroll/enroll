.container.new-course
  .row
    .sidebar.col-sm-3
      %ul.course-progress
        - for step in @steps
          %li{:class => step[:id] == current_step[:id] ? 'active' : ''}
            %a{:href => '#'}= step[:label]
    .col-sm-9
      = simple_form_for(@course, :html => {:class => "new-course form disable-submit-on-return"}) do |form|
        = render "form_header", :form => form
        = render "form_details", :form => form
        .actions
          = form.button(:submit, "Next: #{next_step[:label]}", class: 'btn btn-primary')