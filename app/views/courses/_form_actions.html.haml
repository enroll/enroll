.form-actions
  - if next_step
    = form.button(:submit, "Next: #{next_step[:label]}", class: 'btn btn-primary btn-large')
  - else
    = form.button(:submit, "Finish", class: 'btn btn-primary btn-large')